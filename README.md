### Requirements:

```
* nodejs = v18.16.0
* npm = v9.8.1
* yarn = v1.22.19
```

### Core Dependencies:

```
* "express": "^4.18.2"
* "cors": "^2.8.5"
* "dotenv": "^16.3.1"
* "http-status-codes": "^2.3.0"
* "joi": "^17.10.2"
* "lodash": "^4.17.21"
* "mongodb": "^6.0.0"
```

### Development Tools:

```
* "nodemon": "^3.0.1"                 // Tool to automatically restart the server when code changes
* "eslint": "^8.47.0"                 // Linting tool to maintain code quality and style
* "cross-env": "^7.0.3"               // Set environment variables across different platforms
```

### Babel:

```
* "@babel/runtime": "^7.22.10"                 // Provides polyfills for runtime
* "@babel/cli": "^7.22.10"                     // Babel command-line tools
* "@babel/core": "^7.22.10"                    // Core Babel library for code transformation
* "@babel/eslint-parser": "^7.22.10"           // Babel parser for ESLint
* "@babel/node": "^7.22.10"                    // Run Node.js code with Babel
* "@babel/plugin-transform-runtime": "^7.22.10"// Babel plugin for optimizing runtime
* "@babel/preset-env": "^7.22.10"              // Babel preset to transpile ES6+ code
* "babel-plugin-module-resolver": "^5.0.0"     // Babel plugin to configure module aliases
```

### Miscellaneous:

```
* "async-exit-hook": "^2.0.1"        // Manages asynchronous exit hooks
* "global": "^4.4.0"                 // Library to manage global variables
```